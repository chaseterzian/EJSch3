Vocab

for loops
for ( Initialize  ;     Check       ; Update  )
for (var count = 0; count < exponent; count ++)
____________

Variables
  local
  global

  lexical scoping
    (each local scope, can also see all the local scopes that contain it. All variables from
     blocks around a functions definition are visibile - both those (variables) in fuction bodies
     that enclose it, and those (variables) at the top of the level of the program)
____________

(function) declaration
 these are 2 different ways of declaring this function:

 var square = function(x) {
   return x * x;
 };

 function square(x) {
   return x * X;
 }
____________

q's

What is "return"
what are "side effects"
[1, 2] == [1, 2]  false

Pg 42 - How is this working, most of the chapter relies on the understanding of this function

Functions as values
  pg 45
  again with closure

Defining a function vs Declaring a function

Nesting functions

Recursion - function that calls it'self -
